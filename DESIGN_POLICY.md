# AI外電 デザインポリシー

更新日: 2026-08-11

---

## 位置づけ

本文書はAI外電のデザイン実装における判断基準を定める。**D案**は本リポジトリ内で管理するデザイン体系の名称であり、外部のFigmaファイルやReactプロトタイプを実装上の依存先・正本とはしない。

復元可能な参照実装は次のファイルとする。

- `src/styles/theme.css`: 色、線、角、コンテナ幅などのデザイントークン
- `src/styles/global.css`: 共通レイアウトと主要コンポーネント
- `src/styles/refinement.css`: D案と既存機能を接続する最小限の互換調整
- `src/layouts/BaseLayout.astro`: ヘッダー、主要ナビゲーション、フッター
- `src/pages/index.astro`: トップページの情報階層
- `src/components/NewsCard.astro`: 記事カード
- `src/pages/sources.astro`: 情報源一覧
- `src/pages/articles/[year]/[month]/[day]/[slug].astro`: 個別記事
- `public/default-news-image.png`、`public/default-news-image-1.webp`〜`10.webp`: RSS画像がない記事の既定画像

実装と本文書が衝突する場合は、公開仕様、アクセシビリティ、情報の正確性を確認したうえで本文書を更新し、GitHubだけで判断根拠を復元できる状態を保つ。

---

## 1. 基本思想

信頼性と透明性を前面に出す、編集媒体としての設計。

- 国際的な公共情報サイトのような、落ち着きと明快さ
- 罫線・面・余白・グリッドで情報を整理する
- 派手さより可読性・透明性・再訪しやすさを優先する
- AIを過度に神秘化・未来化しない
- 自動収集、ローカル翻訳、定型編集によるサイトであることを誠実に開示する

---

## 2. 視覚設計

### 色トークン（`src/styles/theme.css`）

| トークン         | 値                 | 用途                         |
| ---------------- | ------------------ | ---------------------------- |
| `--surface`      | `#ffffff`          | ページ背景、カード           |
| `--surface-alt`  | `#eef2f6`          | ヒーロー背景、ページヘッダー |
| `--surface-soft` | `#f5f5f5`          | 画像背景                     |
| `--ink`          | `#1a1a1a`          | 本文テキスト                 |
| `--muted`        | `#6b6b6b`          | 補助テキスト、メタ情報       |
| `--accent`       | `#5a7fa3`          | リンク・強調色               |
| `--accent-dark`  | `#3d6284`          | ホバー・アクティブ           |
| `--accent-soft`  | `#eef2f6`          | バッジ背景                   |
| `--line`         | `rgba(0,0,0,0.13)` | 罫線                         |
| `--line-soft`    | `rgba(0,0,0,0.08)` | 薄い区切り線                 |
| `--radius`       | `0px`              | 角丸なし                     |

差し色は青灰色の1系統へ絞り、高彩度色を複数同時に使わない。

### 線と面

- セクション、カード、ナビゲーションは共通の罫線トークンを使う
- 角丸、影、装飾枠を避ける
- 面と線の開始位置を揃える
- `refinement.css`は互換調整に限定し、テーマトークンや主要レイアウトを上書きしない

### タイポグラフィ

- システムフォントのみ使用し、Webフォントを追加しない
- フォントスタック：`-apple-system, BlinkMacSystemFont, 'Hiragino Kaku Gothic ProN', 'Hiragino Sans', 'Yu Gothic Medium', 'Yu Gothic', 'Meiryo', sans-serif`
- 見出し、本文、補助情報の階層を明確にする

### ファビコン

- 白地に黒いゴシック体の「A」を置く
- 装飾、角丸、複数色を使わない

---

## 3. レイアウト

### ヘッダー

- ロゴ（テキスト）とサブタイトルを左に、実在するナビゲーションリンクを右に置く
- 基準高さは56px
- ブランドマークの四角は使わず、テキストのみとする
- 現在地は文字色と細い下線だけで控えめに示す
- モバイルのナビゲーションは44px相当のタップ領域を確保する
- 全ページ共通の試験運用バーは置かない

### ヒーロー

- 背景は `var(--surface-alt)`
- 見出しは「海外AI公式発表を」「日本語で毎日ダイジェスト」の2行で固定する
- 説明文は公式RSS/Atom、ローカル翻訳、定型編集という実装に一致させる
- 説明文の下に別段落で「試験運用中」と注意事項を表示する
- 右パネル、SVG図、アニメーションは置かない
- 第一画面で最新記事の存在が分かるよう、説明を削らず縦余白を抑える

### 最新ダイジェスト（記事カード）

- 1カラムリスト、外枠ボーダーとカード間罫線で構成する
- PCでは左に4:3のサムネイル、右にメタ情報、タイトル、抜粋、リンクを置く
- サムネイル幅は `clamp(16rem, 32%, 24rem)` とし、1200px級のコンテナでは約32%を占める
- `figure`は4:3を最小比率とし、記事本文が高い場合はカード下端まで伸ばして画像下の空白を作らない
- 画像は `object-fit: cover`、中央基準でトリミングし、枠いっぱいに表示する
- RSS画像のクレジットは画像下端に重ね、4:3の比率を崩さない
- タイトルと抜粋は記事詳細へリンクする
- 英語原題をイタリックで表示する
- タイトルと抜粋は2行を上限とし、PCでは2行分の表示領域を確保する
- 本文領域は4:3画像枠の高さまで伸ばし、原文著者の有無にかかわらずリンク列をカード下端へ揃える
- カード下部に「AI外電の短報を読む」「公式発表を原文で確認」の2リンクを置く
- 「公式発表を原文で確認」はリンク列の右端へ寄せる
- 公式RSS画像は配信元とライセンス情報を表示する

### 既定画像

- RSS画像がない記事では10種類のWebP既定画像を記事IDからランダム風かつ決定的に選んで使用する
- 既定画像は文字・ロゴを含まない、淡い寒色系の抽象AI・ネットワークパターンとする
- 氷河シアン、青緑スレート、ペリウィンクル、寒色セージ、淡いターコイズ、アイスライラック等に色相を振り、明度・彩度・コントラストは同程度に保つ
- 4:3、16:9などへトリミングされても不自然にならない全面パターンを使う
- 同じ記事は記事カードと記事詳細で同じ既定画像を使用する
- 全体OG画像と互換表示用には従来の `default-news-image.png` を維持する
- 記事カードの既定画像は `object-fit: cover`、記事詳細では `object-fit: contain`で中央基準に表示する

### 過去ダイジェスト

- PCでは2列グリッド、モバイルでは1列にする
- 各セルに日付、情報源リスト、件数を別階層で表示する
- 情報源リストは2行までとし、件数はセル右下へ揃える

### 記事詳細ページ

1. パンくず
2. ソースバッジと公開日時
3. h1（日本語タイトル）
4. 英語原題
5. 記事画像と出所・ライセンス
6. 「日本語短報」と本文
7. `ArticleBulletin`
8. 免責注記
9. 公式原文リンク
10. 前後ナビ

RSS画像がある場合はその画像を使用し、ない場合は既定画像を表示する。画像のない記事ページを作らない。

記事詳細の画像はアスペクト比を固定せず、`object-fit: contain`で全体を表示する。画像キャプションは右揃えの補助情報として表示する。

日次ページはSEOタイトルと表示H1を分離し、H1は「2026年8月11日の海外AIニュース（5件）」のように簡潔にする。

日次ページ下部の前後ナビは2列に分け、「前のダイジェスト」「次のダイジェスト」と日本語日付を表示する。片側しかない場合も左右の位置を維持する。

`noindex`、canonical、構造化データ、公開・更新日時等の既存SEO制御をデザイン変更で失わない。

### 情報源ページ

- PCでは2列グリッド、モバイルでは1列にする
- 各カードに名称、累計掲載記事数、取得状態、説明文、カテゴリ、画像取得方針を表示する
- 配信元詳細ページ、公式サイト、公式RSSへのリンクを区別して置く
- 件数を「直近の収集数」や「新着件数」と誤認させない
- 公開済み記事がある配信元を先に置き、0件または一時停止中の配信元は「準備中・一時停止」に分ける

### 過去記事ページ

- PCでは日付と本文領域の2列にする
- タイトルの下へ件数と配信元を補助情報として置く
- 月見出しは「2026年8月」のようにゼロ埋めしない

### フッター

- ブランド、フラットなナビゲーション、著作権・試験運用表記を簡潔に配置する
- ナビ：最新記事 / 過去記事 / 情報源 / このサイトについて / 編集・訂正方針 / プライバシー / RSS
- フッター内では「運営: 外電通信」を重複表示しない
- 著作権・試験運用表記の上に区切り線を置かない

---

## 4. サステナブルな実装

- Astroによる静的HTML生成を維持する
- 不要なJavaScriptや大型UIライブラリを追加しない
- CSSで実現できる表示にJavaScriptを使わない
- Webフォント、動画背景、常時動くアニメーションを避ける
- Pythonパイプラインへ不要な変更を加えない

---

## 5. アクセシビリティ

- WCAG AA相当のコントラスト比を確保する
- 見出し階層、ランドマーク、リスト、ボタン、リンクを意味に沿ったHTMLで実装する
- キーボードだけで主要操作を完了できる
- フォーカス位置を視認できる
- 装飾画像は `alt=""` とし、記事詳細の既定画像には用途を示す代替テキストを付ける
- `prefers-reduced-motion` を尊重する
- PC、タブレット、スマートフォン、200%拡大で横スクロールや情報欠落がないことを確認する

---

## 6. 倫理的なUI

- ダークパターンを使わない
- 自動収集、ローカル翻訳、定型編集を隠さない
- 公式情報、AI外電の短報、外部リンクを明確に区別する
- 未実装の検索、絞り込み、コラム等を操作可能に見せるUIは置かない

---

## 7. レスポンシブ

- 基準ブレークポイントは720px
- 記事カードはモバイルで縦積みにする
- 記事カードの画像枠はモバイルでも4:3を維持する
- 情報源一覧はモバイルで1列にする
- 横スクロールを前提にしない

---

## 8. 実装優先順位

1. セマンティックHTMLと情報階層
2. 公開仕様、データ、SEO制御の保持
3. アクセシビリティと読みやすさ
4. 軽量性と表示速度
5. グリッド、罫線、余白の統一
6. 色とビジュアル表現
7. 装飾や演出

---

## 9. 受け入れ条件

- ライトテーマで白、薄灰、青灰色に整理されている
- 角丸と罫線ルールが共通化されている
- ファビコンが白地・黒字のゴシック体「A」になっている
- システムフォントのみ使用し、Webフォントを追加していない
- 全ページ共通の試験運用バーがなく、トップのヒーロー内に試験運用の説明がある
- トップページの見出しが指定の2行で表示される
- 記事カードがPCで左画像・右本文、モバイルで縦積みになる
- 記事カードのPC画像幅が `clamp(16rem, 32%, 24rem)` になっている
- 記事カードの画像`figure`が最低4:3を保ち、本文が高い場合もカード下端まで画像で埋まる
- 画像が中央トリミングの`cover`で枠いっぱいに表示される
- RSS画像がない記事に文字なしの抽象AIパターンが表示される
- 公式原文リンクが右寄せになる
- RSS画像の出所・ライセンスが表示される
- 情報源ページが2列グリッドで、説明、カテゴリ、取得状態、画像方針、累計件数、各リンクを表示する
- 全記事ページに画像、英語原題、公式原文リンクがあり、既存の`noindex`等を保持する
- 記事詳細の画像全体と右揃えキャプションが表示される
- 日次ページの表示H1がSEOタイトルより簡潔になっている
- 日次ページ下部の前後日付リンクが左右に分離され、44px以上の操作領域を持つ
- 過去記事が日付と「タイトル＋メタ情報」の2列で表示される
- 0件または一時停止中の情報源が別グループへ整理されている
- フッターで運営主体を重複表示せず、著作権表記の上に区切り線がない
- キーボード操作、フォーカス表示、見出し階層、200%拡大を確認できる
- 不要なJavaScript、大型依存、動画背景を追加していない
- GitHub Actions CIとCloudflare Pages Previewが成功している
