# AI外電 デザインポリシー

更新日: 2026-08-06

---

## 位置づけ

本文書はAI外電のデザイン実装における判断基準を定める。デザインの参照実装は **D案**（Reactプロトタイプ）とする。実装時にプロトタイプとこの文書が衝突する場合は、この文書を優先する。画像・アニメーション・フォントの細部よりも、構造・情報階層・アクセシビリティを先に確立する。

---

## 1. 基本思想

信頼性と透明性を前面に出す、編集媒体としての設計。

- 国際的な公共情報サイトのような、落ち着きと明快さ
- 罫線・面・余白・グリッドで情報を整理する
- 派手さより可読性・透明性・再訪しやすさを優先
- AIを過度に神秘化・未来化しない
- 自動収集・自動翻訳サイトであることを誠実に開示する

---

## 2. 視覚設計

### 色トークン（`src/styles/theme.css`）

| トークン | 値 | 用途 |
|---|---|---|
| `--surface` | `#ffffff` | ページ背景、カード |
| `--surface-alt` | `#eef2f6` | ヒーロー背景、ページヘッダー |
| `--surface-soft` | `#f5f5f5` | 画像プレースホルダー |
| `--ink` | `#1a1a1a` | 本文テキスト |
| `--muted` | `#6b6b6b` | 補助テキスト、メタ情報 |
| `--accent` | `#5a7fa3` | リンク・強調色 |
| `--accent-dark` | `#3d6284` | ホバー・アクティブ |
| `--accent-soft` | `#eef2f6` | バッジ背景 |
| `--line` | `rgba(0,0,0,0.13)` | 罫線 |
| `--line-soft` | `rgba(0,0,0,0.08)` | 薄い区切り線 |
| `--radius` | `0px` | 角丸なし（スクエアコーナー） |

差し色は `--accent` / `--accent-dark` の1系統に絞る。高彩度色の複数同時使用は禁止。

### 線と面

- セクション・カード・ナビゲーションは `1px solid var(--line)` で統一
- 角丸・影・グラデーション・装飾枠を避ける
- 面と線の開始位置を揃え、セクションごとに別々のルールを作らない

### タイポグラフィ

- システムフォントのみ使用（Webフォント追加なし）
- フォントスタック：`-apple-system, BlinkMacSystemFont, 'Hiragino Kaku Gothic ProN', 'Hiragino Sans', 'Yu Gothic Medium', 'Yu Gothic', 'Meiryo', sans-serif`
- 見出し・本文・補助情報の階層を明確に

---

## 3. レイアウト

### ヘッダー

- ロゴ（テキスト）＋サブタイトルを左に、ナビゲーションリンクを右に
- 高さ：56px
- ブランドマーク（「外」の四角）なし — テキストのみ

### ヒーロー

- 背景：`var(--surface-alt)` = `#eef2f6`
- コンテンツ：見出し（`SITE_HEADLINE`）＋説明文1段落のみ
- 右パネル・SVG図・アニメーションなし

### 最新ダイジェスト（記事カード）

- 1カラムリスト、外枠ボーダー＋カード間罫線
- 各カード：左にサムネイル画像（固定幅 `10rem`）、右にメタ・タイトル・抜粋・リンク
- タイトルと抜粋テキストは `.news-card__content-link` でまとめて記事詳細へリンク
- 英語原題（`titleOriginal`）をイタリックで表示
- カード下部に「AI外電の短報を読む」「公式発表を原文で確認」の2リンク

### 過去ダイジェスト

- 2列グリッド（`.archive-preview`、PC）、1列（モバイル）
- 各セル：日付・情報源リスト・件数（大きい数字）

### 記事詳細ページ

1. パンくず
2. ソースバッジ＋公開日時
3. h1（日本語タイトル）
4. 英語原題（斜体、`<em>`）
5. サムネイル画像（ある場合、`aspect-ratio: 16/7`）
6. 「日本語短報」h2 + 本文（`briefJa`）
7. `ArticleBulletin`（構造化情報・訂正履歴）
8. 免責注記（プレーンテキスト、ボックスなし）
9. 公式原文リンク（セクション区切り付き）
10. 前後ナビ（3列グリッド）

### 情報源ページ

- 2列グリッド（`.source-grid`）
- 各カード：名称＋件数 / 説明文 / カテゴリタグ / 公式サイト・RSSリンク

### フッター

- 1段（`flex`）：ブランド左、ナビリンク右
- ナビ：最新記事 / 過去記事 / 情報源 / このサイトについて / 編集・訂正方針 / プライバシー / RSS
- 著作権表記を最下行に（`--line-soft` 区切り）

---

## 4. サステナブルな実装

- Astroによる静的HTML生成を維持
- 不要なJavaScriptや大型UIライブラリを追加しない
- CSSで実現できる表示にJavaScriptを使わない
- Webフォント・動画背景・常時動くアニメーションを避ける
- Pythonパイプライン（RSS収集・翻訳）に変更を加えない

---

## 5. アクセシビリティ

アクセシビリティは後付けの検査項目ではなく、初期設計の条件とする。

- WCAG AA相当のコントラスト比を確保
- 見出し階層・ランドマーク・リスト・ボタン・リンクを意味に沿ったHTMLで
- キーボードだけで主要操作を完了できる
- フォーカス位置を視認できる（`outline: 3px solid var(--focus)`）
- 画像に代替テキスト（装飾画像は `alt=""`）
- `prefers-reduced-motion` を尊重

---

## 6. 倫理的なUI

- ダークパターン禁止
- 自動翻訳・AI要約・自動収集を隠さない
- 公式情報・AI外電の短報・外部リンクを明確に区別

---

## 7. レスポンシブ

- 2カラムは必要に応じて1カラムへ（ブレークポイント：720px）
- 記事カード：モバイルでは縦積み（`flex-direction: column`）
- 横スクロール前提にしない

---

## 8. 実装優先順位

1. セマンティックHTMLと情報階層
2. アクセシビリティと読みやすさ
3. 軽量性と表示速度
4. グリッド・罫線・余白の統一
5. 色とビジュアル表現
6. 装飾や演出

---

## 9. 受け入れ条件

- ライトテーマで白・薄灰・`#5a7fa3`青に整理されている
- 角丸（`--radius: 0px`）・罫線ルールが全セクションで共通
- システムフォントのみ使用（Webフォント追加なし）
- トップページの過去ダイジェストが2列グリッド
- 記事カードがサムネイル左・コンテンツ右のflex行レイアウト
- 情報源ページが2列グリッド、各カードに説明文・カテゴリ・リンク
- 記事ページに画像・英語原題・公式原文リンクがある
- キーボード操作・フォーカス表示・見出し階層を確認できる
- 不要なJS・大型依存・動画背景を追加していない
